package "plugin" (
    directory = "."
    version = "dev"
    description = "A Rocq plugin for hash-consing terms"
    requires = "rocq-runtime.plugins.ltac2"
    archive(byte) = "HashconsedReification_plugin.cma"
    archive(native) = "HashconsedReification_plugin.cmxa"
    plugin(byte) = "HashconsedReification_plugin.cma"
    plugin(native) = "HashconsedReification_plugin.cmxs"
)
directory = "."
