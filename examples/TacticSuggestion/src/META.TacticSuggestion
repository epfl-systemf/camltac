package "plugin" (
    directory = "."
    version = "dev"
    description = "A Rocq plugin that provides variants of tactics that suggest a specific argument, like Lean's `apply?`"
    requires = "rocq-runtime.plugins.ltac2"
    archive(byte) = "TacticSuggestion_plugin.cma"
    archive(native) = "TacticSuggestion_plugin.cmxa"
    plugin(byte) = "TacticSuggestion_plugin.cma"
    plugin(native) = "TacticSuggestion_plugin.cmxs"
)
directory = "."
