(library
 (name mltac_plugin)
 (public_name mltac.plugin)
 (synopsis "OCaml as a Tactic Language")
 (libraries
   rocq-runtime.plugins.ltac2
   dynlink
   mltac.plugin.runtime
   mltac.plugin.api)

 ; FIXME: Adding quoting to the libraries stanza fails with "Invalid ELF
 ;        header", so the dependency is added through `preprocess` instead (even
 ;        though we're not pre-processing anything).
 (preprocess (pps mltac.plugin.quoting)))

(rocq.pp
 (modules grammar))
